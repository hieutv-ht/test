on:
  push:
     branches: [ master ]
jobs:
  setup:
    name: Setup Env
    runs-on: ubuntu-latest
    steps:
      - name: abc
        env:
          VERSION_CODE: ${{ secrets.VERSION_CODE }}
        run: echo TEST $VERSION_CODE